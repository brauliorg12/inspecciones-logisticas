import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'inspecciones-logisticas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
